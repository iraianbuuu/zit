export enum DEFAULT_FILES_PATH {
    HEAD = '../config/default-files/HEAD',
    CONFIG = '../config/default-files/config',
    EXCLUDE = '../config/default-files/exclude',
    DESCRIPTION = '../config/default-files/description'
}

export enum GIT_FILES {
    HEAD = 'HEAD',
    CONFIG = 'config',
    DESCRIPTION = 'description',
    EXCLUDE = 'exclude'
}

export enum GIT_FOLDERS {
    HOOKS = 'hooks',
    INFO = 'info',
    OBJECTS = 'objects',
    PACK = 'pack',
    REFS = 'refs'
}